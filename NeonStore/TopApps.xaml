<Page
    x:Name="pageRoot"
    x:Class="NeonStore.TopApps"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NeonStore"
    xmlns:common="using:NeonStore.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">My Application</x:String>
        <Storyboard x:Name="SlideInStoryboard">

            <!-- Slide animation -->
            <DoubleAnimation
            Storyboard.TargetName="MainPanelTransform"
            Storyboard.TargetProperty="X"
            From="200" To="0"
            Duration="0:0:1.2"
            EnableDependentAnimation="True">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid>
        <Grid.Background>
            <ImageBrush Stretch="UniformToFill" ImageSource="ms-appx:///Assets/Store1.png"/>
        </Grid.Background>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="Top Apps" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>
        <!-- CONTENT AREA -->
        <Grid Grid.Row="1">

            <ScrollViewer
        HorizontalScrollMode="Auto"
        HorizontalScrollBarVisibility="Auto"
        VerticalScrollMode="Disabled"
        VerticalScrollBarVisibility="Disabled"
        ZoomMode="Disabled">

                <GridView x:Name="ProjectsGrid"
              IsItemClickEnabled="True"
              ItemClick="ProjectsGrid_ItemClick"
              ItemsSource="{Binding}"
              Margin="40,10,0,100">
                    <GridView.RenderTransform>
                        <TranslateTransform x:Name="MainPanelTransform"
                            X="200"/>
                    </GridView.RenderTransform>

                    <!-- WRAP LAYOUT -->
                    <GridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsWrapGrid Orientation="Vertical"/>
                        </ItemsPanelTemplate>
                    </GridView.ItemsPanel>

                    <GridView.ItemTemplate>
                        <DataTemplate>

                            <!-- Windows 8 Style Tile -->
                            <Grid Width="250"
              Height="220"
              Margin="8"
              Background="Transparent">

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="140"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- IMAGE (main visual focus) -->
                                <Border Grid.Row="0"
                    Background="#202020">

                                    <Image Source="{Binding DetailImagePath}"
                       Stretch="UniformToFill"/>

                                </Border>

                                <!-- TEXT AREA -->
                                <StackPanel Grid.Row="1"
                        Margin="10,8,10,8">

                                    <!-- TITLE -->
                                    <TextBlock Text="{Binding Title}"
                           FontSize="16"
                           FontWeight="Light"
                           Foreground="White"
                           TextTrimming="CharacterEllipsis"
                           MaxLines="2"/>

                                    <!-- CATEGORY (NEW - Windows 8 style metadata) -->
                                    <TextBlock Text="{Binding Category}"
                           FontSize="11"
                           Foreground="{StaticResource AccentBrush}"
                           Margin="0,4,0,0"
                           FontWeight="Light"/>

                                    <!-- PUBLISHER -->
                                    <TextBlock Text="{Binding Publisher}"
                           FontSize="11"
                           Foreground="#BFBFBF"
                           Margin="0,3,0,0"
                           TextTrimming="CharacterEllipsis"/>

                                    <!-- DESCRIPTION -->
                                    <TextBlock Text="{Binding Description}"
                           FontSize="11"
                           Foreground="#8C8C8C"
                           Margin="0,5,0,0"
                           TextWrapping="Wrap"
                           MaxLines="3"
                           TextTrimming="CharacterEllipsis"/>

                                </StackPanel>

                            </Grid>

                        </DataTemplate>
                    </GridView.ItemTemplate>
                    <GridView.ItemContainerStyle>

                        <Style TargetType="GridViewItem">

                            <!-- NORMAL -->
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="VerticalContentAlignment" Value="Stretch"/>

                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="GridViewItem">

                                        <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            RenderTransformOrigin="0.5,0.5">

                                            <Border.RenderTransform>
                                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                                            </Border.RenderTransform>

                                            <ContentPresenter />

                                            <VisualStateManager.VisualStateGroups>

                                                <VisualStateGroup x:Name="CommonStates">

                                                    <!-- NORMAL -->
                                                    <VisualState x:Name="Normal"/>

                                                    <!-- POINTER OVER (HOVER) -->
                                                    <VisualState x:Name="PointerOver">
                                                        <Storyboard>

                                                            <DoubleAnimation Storyboard.TargetName="Root"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0.85"
                                                         Duration="0:0:0.1"/>

                                                            <DoubleAnimation Storyboard.TargetName="Root"
                                                         Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                                         To="1.05"
                                                         Duration="0:0:0.1"/>

                                                            <DoubleAnimation Storyboard.TargetName="Root"
                                                         Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                                         To="1.05"
                                                         Duration="0:0:0.1"/>

                                                        </Storyboard>
                                                    </VisualState>

                                                    <!-- PRESSED -->
                                                    <VisualState x:Name="Pressed">
                                                        <Storyboard>

                                                            <DoubleAnimation Storyboard.TargetName="Root"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0.6"
                                                         Duration="0:0:0.05"/>

                                                        </Storyboard>
                                                    </VisualState>

                                                </VisualStateGroup>

                                            </VisualStateManager.VisualStateGroups>

                                        </Border>

                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>

                        </Style>

                    </GridView.ItemContainerStyle>

                </GridView>
            </ScrollViewer>

        </Grid>
    </Grid>
    <Page.BottomAppBar>
        <AppBar Background="{StaticResource AccentBrush}" IsSticky="True" IsOpen="True">

            <Grid Background="{StaticResource AccentBrush}">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- LEFT SIDE: HEADER + BUTTONS -->
                <StackPanel Grid.Column="0"
                        Orientation="Horizontal"
                        VerticalAlignment="Center">

                    <!-- HEADER -->
                    <StackPanel Orientation="Horizontal"
                            Margin="10,10,10,5"
                            VerticalAlignment="Center">

                        <Image Source="ms-appx:///Assets/SmallLogo.png"
                           Width="30"
                           Height="30"
                           Margin="10,0,10,0"/>

                        <TextBlock Text="NeonStore"
                               Foreground="White"
                               FontSize="18"
                               VerticalAlignment="Center"
                               FontWeight="Light"/>
                    </StackPanel>

                    <!-- BUTTONS (still LEFT) -->
                    <AppBarButton Icon="Refresh"
                              Label="Refresh"
                              Click="Refresh_Click"/>

                </StackPanel>

                <!-- MIDDLE SPACER -->
                <Grid Grid.Column="1"/>

                <!-- RIGHT SIDE: SEARCH ONLY -->
                <StackPanel Grid.Column="2"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        Margin="0,0,10,0">

                    <SearchBox x:Name="SearchBox" QueryChanged="SearchBox_QueryChanged"
QuerySubmitted="SearchBox_QuerySubmitted"
                           Width="300"
                           Height="32"
                           PlaceholderText="Search top apps..." SearchHistoryEnabled="False"
                           Margin="0,0,0,0"/>

                </StackPanel>

            </Grid>

        </AppBar>
    </Page.BottomAppBar>
</Page>
