<SettingsFlyout
    x:Class="NeonStore.Updates"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NeonStore"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    IconSource="Assets/SmallLogo.png"
    Title="Updates"
    d:DesignWidth="346" HeaderBackground="{StaticResource AccentBrush}">
    <SettingsFlyout.Background>
        <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
            <GradientStop Color="#E9F0F3" Offset="0"/>
            <GradientStop Color="#A2A5AC" Offset="1"/>
        </LinearGradientBrush>
    </SettingsFlyout.Background>

    <!-- This StackPanel acts as a root panel for vertical layout of the content sections -->
    <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >

        <!-- The StackPanel(s) below define individual content sections -->

        <!-- Content Section 1-->
        <StackPanel Style="{StaticResource SettingsFlyoutSectionStyle}">

            <!-- Section 1 header -->
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Updates" FontSize="28"/>

            <!-- Section 1 body -->
            <StackPanel>

                <TextBlock Style="{StaticResource BodyTextBlockStyle}"
               Margin="0,0,0,15"
               TextWrapping="Wrap"
               Text="Keep your app updated for the latest features and fixes." />

                <Button Content="Check for updates"
            Click="CheckForUpdates_Click"
            HorizontalAlignment="Left"
            Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                    <!-- Progress Ring -->
                    <ProgressRing x:Name="UpdateProgressRing"
                  Width="20"
                  Height="20"
                  IsActive="False"
                  Margin="0,0,10,0"/>

                    <!-- Status Text -->
                    <TextBlock x:Name="UpdateStatusText"
               Style="{StaticResource BodyTextBlockStyle}"
               Text="Ready to check for updates"
               TextWrapping="Wrap"/>
                </StackPanel>
            </StackPanel>

        </StackPanel>

        <!-- Define more Content Sections below as necessary -->

    </StackPanel>
</SettingsFlyout>
