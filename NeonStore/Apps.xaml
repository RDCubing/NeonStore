<Page
    x:Name="pageRoot"
    x:Class="NeonStore.Apps"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NeonStore"
    xmlns:common="using:NeonStore.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">All Apps</x:String>
    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid>
        <Grid.Background>
            <ImageBrush Stretch="UniformToFill" ImageSource="ms-appx:///Assets/Store1.png"/>
        </Grid.Background>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="All Applications" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>
        <!-- CONTENT AREA -->
        <Grid Grid.Row="1">

            <ScrollViewer
        HorizontalScrollMode="Auto"
        HorizontalScrollBarVisibility="Auto"
        VerticalScrollMode="Disabled"
        VerticalScrollBarVisibility="Disabled"
        ZoomMode="Disabled">

                <GridView x:Name="ProjectsGrid" IsItemClickEnabled="True" ItemClick="ProjectsGrid_ItemClick"
          ItemsSource="{Binding}"
          Margin="40,40,40,100">

                    <!-- ✅ Horizontal + only 2 rows -->
                    <GridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsWrapGrid Orientation="Vertical"/>
                        </ItemsPanelTemplate>
                    </GridView.ItemsPanel>

                    <GridView.ItemTemplate>
                        <DataTemplate>

                            <!-- ✅ Bigger item -->
                            <Grid Height="125" Width="375" Margin="10">

                                <Grid.ColumnDefinitions>
                                    <!-- ✅ square image -->
                                    <ColumnDefinition Width="125"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Image -->
                                <Border Grid.Column="0"
                        Width="125"
                        Height="125">

                                    <Image Source="{Binding ImagePath}"
                           Stretch="UniformToFill"/>
                                </Border>

                                <!-- Text -->
                                <Border Grid.Column="1"
                        Background="#33000000"
                        Padding="16">

                                    <StackPanel VerticalAlignment="Center">

                                        <TextBlock Text="{Binding Title}"
                                   Foreground="White"
                                   FontSize="20"
                                   FontWeight="SemiLight"
                                   TextWrapping="Wrap"/>

                                        <TextBlock Text="{Binding Subtitle}"
                                   Foreground="#CCCCCC"
                                   FontSize="13"
                                   Margin="0,6,0,0"
                                   TextWrapping="Wrap"/>
                                        <!-- ✅ NEW: Publisher -->
                                        <TextBlock Text="{Binding Publisher}"
               Foreground="#88FFFFFF"
               FontSize="11"
               Margin="0,4,0,0"
               TextWrapping="Wrap"/>

                                    </StackPanel>

                                </Border>

                            </Grid>

                        </DataTemplate>
                    </GridView.ItemTemplate>

                </GridView>

            </ScrollViewer>

        </Grid>
    </Grid>
    <Page.BottomAppBar>
        <AppBar IsSticky="True" IsOpen="True">

            <Grid Background="#0F4C4C">

                <StackPanel Orientation="Horizontal">

                    <!-- HEADER (Logo + Title) -->
                    <StackPanel Orientation="Horizontal"
                    Margin="10,10,10,5"
                    VerticalAlignment="Center">

                        <!-- Logo -->
                        <Image Source="Assets/StoreLogo.png"
                   Width="50"
                   Height="30"
                   Margin="0,0,8,0"/>

                        <!-- Title -->
                        <TextBlock Text="NeonStore"
                       Foreground="White"
                       FontSize="18"
                       VerticalAlignment="Center"
                       FontWeight="Light"/>

                    </StackPanel>

                    <Button Click="Refresh_Click"
                    Width="Auto"
                    Height="64" Margin="10"
                    BorderThickness="0">

                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">

                            <SymbolIcon Symbol="Refresh"
                                 HorizontalAlignment="Center"/>

                            <TextBlock Text="Refresh"
                               HorizontalAlignment="Center"/>

                        </StackPanel>

                    </Button>
                </StackPanel>

            </Grid>

        </AppBar>
    </Page.BottomAppBar>
</Page>
